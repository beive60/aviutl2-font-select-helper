# 対象コンボボックス動的特定ロジック ソフトウェア要件定義 (SRS)

## 1. 概要と目的

AviUtl2（64bit）の拡張編集エンジンが生成するオブジェクト設定ウィンドウにおいて、フォント選択用コンボボックス（CBS_DROPDOWNLIST）のウィンドウハンドル（HWND）を動的かつ不可逆的に特定する。静的なコントロールIDへの依存を完全に排除し、データ駆動型のヒューリスティック評価を適用することで、将来的なUIレイアウト変更に対する高度なプラットフォーム非依存性を確保する。

## 2. システムアーキテクチャ（技術スタック）

本コンポーネントは、メモリ安全性と並行処理の堅牢性を担保するため、以下の技術スタックを用いて実装される。

言語: Rust (Edition 2021以降)

ベースSDK: aviutl2-rs (ホストプロセスへのアタッチ、プラグインライフサイクルの管理)

システムインターフェース: windows クレート (Win32 API、フック、GDIリソース管理)

エンコーディング: encoding_rs または同等のクレート (UTF-16LE / CP932 / UTF-8 間の安全な境界変換)

## 3. 機能要件

### 3.1. イベント検知と走査トリガー

aviutl2-rsのプラグイン初期化ルーチンを起点とし、オブジェクト設定ウィンドウの生成プロセス（WM_INITDIALOGまたはWM_CREATE）をグローバルフック（SetWindowsHookEx / WH_CALLWNDPROCRET等）によって捕捉する。

捕捉した親ウィンドウのハンドルを基点とし、EnumChildWindows APIを発行して子コントロール群を列挙する。

### 3.2. 対象コントロールの事前フィルタリング

列挙された子コントロール群から、ウィンドウクラス名がComboBoxであるものを抽出する。

抽出された各コントロールに対し、CB_GETCOUNTメッセージを送信し、リスト項目数を取得する。

項目数が規定の要件（システムフォント総数との相対評価）を満たさないコントロールは検証対象外として即座に除外する。

### 3.3. データ駆動型ヒューリスティック評価（サンプリング検証）

フィルタリングを通過したコントロールに対し、CB_GETLBTEXTメッセージを送信し、インデックス0番からN番の文字列データを抽出する。

抽出データ（CP932）をUTF-8に変換し、プラグイン初期化時にOSから取得・キャッシュした有効なシステムフォント名リスト（HashSet<String>等）と照合する。

サンプリングした全項目がフォント名リストと一致した場合、当該コントロールを「フォント選択用コンボボックス」と断定し、対象のHWNDとして確定する。

### 3.4. オーバーレイ適用の実行

確定したHWNDに対し、GetWindowRectおよびScreenToClientを用いて親ウィンドウ上の相対座標を取得する。

特定された標準コンボボックスに対し、ShowWindow(hwnd, SW_HIDE)を実行して不可視状態へ移行させる。

取得した座標情報に基づき、インクリメンタルサーチ対応のカスタムUIコンポーネント（Win32 Edit / ListBox）をオーバーレイ生成する。

## 4. 非機能要件

### 4.1. パフォーマンスおよび実行コンテキスト要件

ホストスレッドの非ブロッキング: プラグインの初期化フェーズにおけるOSからのフォントリスト列挙処理は、非同期タスクとしてバックグラウンドで実行し、遅延評価（Lazy Initialization）させる。

UI操作のスレッド制約（アフィニティの順守）: オーバーレイUIの構築、および対象コンボボックスへのメッセージ送受信は、必ずホストのメインUIスレッド上から実行する。Rustの型システム（Send / Syncの非実装）を利用し、非同期スレッドからの直接的なUI操作をコンパイルレベルで禁止する。

検索アルゴリズムの計算量: O(1)での検索が可能なハッシュセット（std::collections::HashSet等）を用い、メインスレッドの処理系を滞留させないレイテンシを保証する。

### 4.2. 堅牢性要件（保守性および安全性）

コントロールID、ダイアログ内のZオーダーなど、UIレイアウトに依存する静的パラメータへのハードコードを一切禁止する。

FFI境界のパニック隔離: 全てのフックプロシージャ関数およびサブクラス化されたウィンドウプロシージャの実装は、std::panic::catch_unwindでラップする。Rust側のパニックがC ABI境界を越えてホスト（aviutl2.exe）をクラッシュさせることを防止する。

リソース管理: 生成されたHWNDやGDIオブジェクト（HBRUSH, HFONT等）、およびフックのハンドル（HHOOK）は、RustのDropトレイトを実装したラッパー構造体にて管理し、ライフサイクル終了時に確実に解放（RAIIパターン）する。

## 5. リスクおよび制約事項と対策

リスク: プラグイン側の内部処理を担う非同期スレッドから、メインスレッドに属するコントロールへ対して同期的な操作を行った場合、コンテキストスイッチによるデッドロックが発生する。

対策: スレッド境界を越える状態更新が必要な場合は、メインスレッド側のメッセージループにフックしたディスパッチャを経由させるか、非同期のPostMessageを用いたタスクキュー機構を構築する。

リスク: プロセス終了時に非同期スレッドが稼働し続けていることによるメモリの二重解放やアクセス違反。

対策: aviutl2-rsの提供する破棄・終了コールバック、またはメインウィンドウのWM_DESTROYフックに連動した、ティアダウン・シーケンス（スレッドのJoinおよびフックの解除）を実装する。
